fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.5
xlrd==2.0.1
scikit-learn==1.5.1
statsmodels==0.14.2
prophet==1.1.5
holidays==0.57
# xgboost==2.1.1 est utilisé par app/services/forecasting_service.py (modele=xgboost)
# mais n'est PAS listé ici : il doit être installé séparément avec --no-deps pour
# éviter que son wheel PyPI n'entraîne nvidia-nccl-cu12 (~340 Mo, GPU multi-carte,
# jamais utilisé ici). Cf. Dockerfile et README §7.b pour la commande exacte —
# ne pas le rajouter tel quel ici sous peine de réintroduire ce téléchargement
# inutile au premier `pip install -r requirements.txt`.
weasyprint==69.0
matplotlib==3.10.8
python-multipart==0.0.9
httpx==0.27.2
pytest==8.3.2
faker==26.0.0
bcrypt==5.0.0
python-jose[cryptography]==3.3.0
